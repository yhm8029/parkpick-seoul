# ParkPick Seoul 제품 동작 명세

이 문서는 현재 배포된 0.2.x 런타임의 사용자 관찰 가능 동작을 정의합니다.

## 1. 입력과 위치 권한

- 출발지는 GPS 또는 장소검색으로 선택합니다.
- 브라우저 위치 권한은 `현재 위치 사용` 또는 새로고침 버튼을 누른 뒤에만 요청합니다.
- GPS 정확도는 브라우저와 기기가 제공한 `accuracy`를 그대로 표시하며 서비스가 임의로 높이지 않습니다.
- 목적지는 NAVER 장소검색 결과에서 선택합니다. 고정된 예시 장소 버튼은 표시하지 않습니다.
- 도착 예정시간, 예상 체류 시간, 추천 기준과 탐색 반경을 변경하면 기존 결과와 진행 중 요청을 무효화합니다.

## 2. 장소검색 우선순위

`GET /api/places/search?q=<검색어>`는 다음 순서로 동작합니다.

1. `NAVER_API_HUB_KEY_ID`와 `NAVER_API_HUB_KEY`가 있으면 NAVER API HUB 지역검색
2. 결과가 없거나 호출이 실패하면 NAVER Maps Geocoding
3. 사용 가능한 실제 결과가 없으면 저장소의 데모 장소검색

검색 결과는 이름, 주소, 위도, 경도, 카테고리와 `NAVER` 또는 `DEMO` 출처로 정규화됩니다. 비밀키와 원본 공급자 응답은 브라우저로 전달하지 않습니다.

## 3. 추천 요청

`POST /api/recommendations`는 다음 값을 받습니다.

- 출발지 좌표
- 선택한 목적지
- ISO 형식 도착 예정시간
- 30~1,440분 체류시간
- `BALANCED`, `CHEAP`, `NEAR`, `CERTAIN` 추천 기준
- `AUTO` 또는 최대 거리 50~1,000m의 `MANUAL` 탐색

입력은 서버에서 검증·보정됩니다. 추천 요청은 캐시하지 않으며 최신 요청의 응답만 UI 상태에 반영합니다.

## 4. 주차장 후보와 추천

1. 기본 소스인 서울 주차 포털 근접 검색에서 목적지 반경 안 후보를 가져옵니다.
2. 포털 유형 코드가 공영 노상(`NS`), 공영 노외(`NW`), 공공시설 부설(`BP`)인 항목만 허용합니다. 일반 부설(`BS`)과 민영 노외(`NP`)를 포함한 나머지 유형은 제외합니다.
3. `AUTO`는 1km 안에서 목적지와 가장 가까운 최대 10곳을 먼저 고정한 뒤 점수를 계산합니다. `MANUAL`은 사용자가 정한 반경 안 전체 후보를 점수화하고 상위 10곳을 고릅니다.
4. 고정한 후보 최대 10곳만 NAVER Directions 5로 현재 교통 경로를 병렬 조회합니다.
5. 같은 후보 집합 안에서 경로 결과를 반영해 최종 순위를 다시 계산합니다.

후보별 경로는 최대 2,500개 좌표와 256개 혼잡 구간으로 제한합니다. 추천 응답 전체의 상한은 좌표 25,000개와 혼잡 구간 2,560개이며, 공급자 호출 실패는 해당 후보만 `ESTIMATE`로 대체합니다.

서울 주차 포털 호출 자체가 실패한 경우에만 `SEOUL_OPEN_API_KEY`가 있으면 GetParkInfo/GetParkingInfo 대체 소스를 사용합니다. 키가 없거나 대체 소스도 실패하면 503을 반환합니다. 정상 응답의 후보가 적거나 0건인 경우 데모 주차장을 섞지 않습니다.

## 5. 이동시간과 경로의 의미

- `주차장까지 자동차`: 출발지 → 주차장입니다.
- `NAVER_DIRECTIONS`: NAVER Directions 5 `trafast`의 요청 시점 교통시간, 도로거리와 경로선입니다.
- `ESTIMATE`: 공급자 실패 시 직선거리 기반 자동차 추정값입니다.
- `목적지까지 도보`: 주차장 → 목적지 직선거리를 기반으로 한 보행시간 추정입니다. 실제 보행 경로나 횡단 가능 여부를 보장하지 않습니다.
- 지도는 선택한 카드의 NAVER 경로선과 제공되는 혼잡 구간을 표시합니다.
- 카드 변경 시 경로 오버레이만 교체하고 사용자의 지도 줌·팬 상태는 유지합니다.

## 6. 빈자리와 요금

- 실시간 빈자리는 서울 주차 포털이 실시간 사용면을 제공하는 주차장만 계산합니다.
- 실시간 미지원 주차장은 `확인 불가`와 공급자 미제공 안내를 표시합니다.
- 공공시설 부설(`BP`)은 `공공시설 부설` 배지를 표시합니다. 운영시간이나 방문 목적에 따른 이용조건은 현장에서 확인해야 합니다.
- 빈자리 예측 범위는 현재 값, 데이터 나이, 도착까지의 시간과 추세가 있을 때만 계산합니다.
- 예상요금은 체류시간과 기본/추가 단위 요금, 무료 여부, 일 최대요금을 반영합니다.
- 카드에는 무료, 동일 단가, 구간별 단가 또는 단위 정보 없음 상태를 구분해 표시합니다.

## 7. 화면 상태

- 입력 모드에서는 방문 계획과 NAVER 지도를 표시합니다.
- 추천 성공 시 입력 패널을 추천 카드 목록으로 교체합니다.
- 최대 10곳을 반환하되 처음에는 카드와 지도 마커를 상위 3곳만 표시합니다. `더 보기`와 `접기`는 카드와 마커를 함께 전환합니다.
- `조건 변경`을 누르면 입력값을 보존한 채 입력 모드로 돌아갑니다.
- 모바일에서는 목록과 지도를 전환하며 성공 직후 목록을 우선 표시합니다.
- 결과 제목으로 포커스를 이동하고 추천 개수를 `role=status`로 알립니다.
- 오류 또는 빈 추천이면 입력 모드를 유지하고 사용자가 다시 시도할 수 있는 고정 안내를 표시합니다.

## 8. 지도와 길안내

- 목적지 선택 전에도 서울 중심 또는 출발지 중심의 NAVER Web Dynamic Map을 표시합니다.
- 출발지, 목적지와 현재 화면에 표시된 추천 주차장 1~10곳을 마커로 표시합니다.
- 현재 런타임 UI에는 NAVER 공급자만 노출합니다.
- 길안내는 네이버지도 URL Scheme/Intent를 우선 사용하고 설치되지 않은 환경에서는 웹/스토어 폴백을 제공합니다.
- 잠재적 향후 지원을 위한 Kakao 구현은 active import graph 밖의 dormant source로만 보존합니다.

## 9. 개인정보와 분석

- GPS 좌표는 지도 표시, 추천·NAVER 경로 계산과 사용자가 누른 네이버지도 길안내 handoff에 사용합니다. 브라우저 저장소나 프로젝트 데이터베이스에는 저장하지 않습니다.
- 서비스 자체 사용자 계정이나 위치 이력 저장 기능은 없습니다.
- Vercel Web Analytics를 활성화한 경우 익명 집계값만 `TODAY`와 `30 DAYS`로 표시합니다.
- 분석 API가 없거나 실패하면 방문자 블록을 숨기며 가짜 0을 표시하지 않습니다.

## 10. 현재 제외 범위

- 주차 가능 보장 또는 예약·결제
- 실제 보행 경로 API
- 과거 주차 이력 기반 예측 모델
- 사용자 위치 이력 저장
- Kakao 지도·검색·길찾기의 현재 런타임 지원
